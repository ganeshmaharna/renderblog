{
  "name": "server",
  "version": "1.0.0",
  "description": "This will do backend work for blog app",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "client-install":"cd client && npm install",
    "client-build":"cd client && npm run build",
    "render-postbuild": "npm run client-install && npm run client-build"
  },
  "author": "Ganesh",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.0",
    "body-parser": "^1.20.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "gridfs-stream": "^1.1.1",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^6.8.2",
    "multer": "^1.4.5-lts.1",
    "multer-gridfs-storage": "^5.0.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
